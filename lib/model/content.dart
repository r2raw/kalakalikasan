class Content {

  const Content(this.images,this.title, this.description, this.timeAgo);
  final List images;
  final String description;
  final String title;
  final Map<String, dynamic> timeAgo;
  // final int num_of_comments;
  // final int num_of_reacts;
}